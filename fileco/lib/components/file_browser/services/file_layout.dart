import 'package:flutter/material.dart';

class FileLayout extends StatefulWidget {
  const FileLayout({super.key});

  @override
  State<FileLayout> createState() => _FileLayoutState();
}

class _FileLayoutState extends State<FileLayout> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

//TODO:I've to implement layout for files,directories across the whole app 
//TODO: I've to implement board, list, small icons, large icons, table if possible