import 'package:flutter/material.dart';

class IconHolder extends StatelessWidget {
  const IconHolder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
       decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(20)
       ),
     // child: Icon(),
    );
  }
} 